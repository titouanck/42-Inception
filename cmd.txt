apk add mysql mysql-client

rc-service mariadb setup
rc-service mariadb start
mysql_secure_installation
mysql -u root

mysqld --user=mysql --console


sudo docker inspect -f '{{.Name}} - {{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' $(sudo docker ps -q)



CREATE DATABASE ticodb;
CREATE USER 'tico45'@'%' IDENTIFIED BY 'tico45';
GRANT ALL PRIVILEGES ON ticodb.* TO 'tico45'@'%' IDENTIFIED BY 'tico45';
172.29.0.2




Un Makefile est également requis et doit être situé à la racine de votre répertoire. Il doit configurer
l'intégralité de votre application (c'est-à-dire qu'il doit créer les images Docker à l'aide de dockercompose.yml).

Pour des raisons de performances, les conteneurs doivent être construits soit à partir de l'avant-dernière
version stable d'Alpine, soit à partir de Debian. Le choix t'appartient.
// check les versions